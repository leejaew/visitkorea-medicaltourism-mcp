[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Production-grade MCP server for Korea Medical Tourism data"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.27.0",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
]
